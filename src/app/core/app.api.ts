/**
 * List of the user APIs of the back-end
 */
export declare const enum UserAPIS {
    USER_REGISTER = 'users',
    GET_USERS = 'getUser',
}
/**
 * List of the auth APIs of the back-end
 */
export declare const enum AuthAPIS {
    AUTHENTICATE = 'auth',
    REFRESH_TOKEN = 'auth/refreshToken',
}
/**
 * List of the eclass APIs of the back-end
 */
export declare const enum EclassAPIS {
    GET_STRUCTURED_DATA = 'eclass/getStructuredData',
    GET_FIELDS = 'eclass/getFields',
}
